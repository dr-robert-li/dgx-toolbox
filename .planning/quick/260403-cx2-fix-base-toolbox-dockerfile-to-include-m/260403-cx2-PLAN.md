---
phase: quick
plan: 260403-cx2
type: execute
wave: 1
depends_on: []
files_modified:
  - base-toolbox/Dockerfile
autonomous: true
must_haves:
  truths:
    - "base-toolbox image includes transformers, trl, peft, accelerate, sentencepiece, hf_transfer, pyyaml"
    - "keras_nlp/keras/keras-core conflict is removed before transformers install"
    - "downstream images (eval-toolbox, data-toolbox) inherit all ML deps without changes"
  artifacts:
    - path: "base-toolbox/Dockerfile"
      provides: "Base image with ML training deps and keras_nlp conflict fix"
      contains: "pip uninstall.*keras"
  key_links:
    - from: "base-toolbox/Dockerfile"
      to: "eval-toolbox/Dockerfile"
      via: "FROM base-toolbox:latest"
      pattern: "FROM base-toolbox"
    - from: "base-toolbox/Dockerfile"
      to: "data-toolbox/Dockerfile"
      via: "FROM base-toolbox:latest"
      pattern: "FROM base-toolbox"
---

<objective>
Add missing ML training dependencies (transformers, trl, peft, accelerate, sentencepiece, hf_transfer, pyyaml) to the base-toolbox Dockerfile and remove the keras_nlp backend conflict so all downstream containers inherit a working ML stack.

Purpose: The NGC PyTorch base image ships a keras_nlp stub that conflicts with transformers 4.56+. Removing it and pre-installing common ML deps means unsloth-headless, eval-toolbox, and data-toolbox all get a working environment without per-container workarounds.
Output: Updated base-toolbox/Dockerfile
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@base-toolbox/Dockerfile
@eval-toolbox/Dockerfile
@data-toolbox/Dockerfile
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add keras_nlp removal and ML training deps to base-toolbox Dockerfile</name>
  <files>base-toolbox/Dockerfile</files>
  <action>
Edit base-toolbox/Dockerfile to:

1. Add a NEW RUN layer BEFORE the existing pip install block that removes the keras_nlp conflict:
   ```
   RUN pip uninstall -y keras-nlp keras keras-core 2>/dev/null || true
   ```
   This must run before any pip install of transformers to avoid the backend conflict.

2. Add the following packages to the existing pip install block (the one that already has datasets, scipy, openai, etc.):
   - "transformers"
   - "accelerate"
   - "peft"
   - "trl"
   - "sentencepiece"
   - "hf_transfer"
   - "pyyaml"

   Keep existing packages unchanged. Group the new ML training packages with a comment separating them from the existing data/utility packages for readability.

Do NOT change the base image tag (26.02-py3), the system apt-get layer, or the ENTRYPOINT.
Do NOT add version pins — let pip resolve compatible versions against the NGC base.
  </action>
  <verify>
    <automated>cd /home/robert_li/dgx-toolbox && grep -q "keras-nlp" base-toolbox/Dockerfile && grep -q "transformers" base-toolbox/Dockerfile && grep -q "accelerate" base-toolbox/Dockerfile && grep -q "peft" base-toolbox/Dockerfile && grep -q "trl" base-toolbox/Dockerfile && grep -q "sentencepiece" base-toolbox/Dockerfile && grep -q "hf_transfer" base-toolbox/Dockerfile && grep -q "pyyaml" base-toolbox/Dockerfile && echo "ALL DEPS PRESENT"</automated>
  </verify>
  <done>base-toolbox/Dockerfile contains keras_nlp removal step and all 7 new ML training packages. File parses as valid Dockerfile syntax.</done>
</task>

</tasks>

<verification>
- All 7 new packages appear in base-toolbox/Dockerfile pip install
- keras_nlp uninstall line appears BEFORE pip install of transformers
- eval-toolbox/Dockerfile and data-toolbox/Dockerfile are NOT modified (they inherit via FROM base-toolbox:latest)
- Dockerfile has no syntax errors (each RUN ends properly, backslash continuations correct)
</verification>

<success_criteria>
- base-toolbox/Dockerfile includes keras removal and all ML training deps
- No changes to downstream Dockerfiles
- Dockerfile builds without syntax issues (validated by visual inspection of layer structure)
</success_criteria>

<output>
After completion, create `.planning/quick/260403-cx2-fix-base-toolbox-dockerfile-to-include-m/260403-cx2-SUMMARY.md`
</output>
