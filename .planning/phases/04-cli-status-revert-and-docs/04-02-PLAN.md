---
phase: 04-cli-status-revert-and-docs
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - inference/start-open-webui.sh
  - inference/start-open-webui-sync.sh
  - inference/start-vllm.sh
  - inference/start-vllm-sync.sh
  - inference/start-litellm.sh
  - inference/start-litellm-sync.sh
  - inference/setup-litellm-config.sh
  - inference/setup-ollama-remote.sh
  - data/data-toolbox.sh
  - data/data-toolbox-build.sh
  - data/data-toolbox-jupyter.sh
  - data/start-label-studio.sh
  - data/start-argilla.sh
  - eval/eval-toolbox.sh
  - eval/eval-toolbox-build.sh
  - eval/eval-toolbox-jupyter.sh
  - eval/triton-trtllm.sh
  - eval/triton-trtllm-sync.sh
  - containers/ngc-pytorch.sh
  - containers/ngc-jupyter.sh
  - containers/ngc-quickstart.sh
  - containers/unsloth-studio.sh
  - containers/unsloth-studio-sync.sh
  - containers/start-n8n.sh
  - setup/dgx-global-base-setup.sh
  - modelstore/lib/hf_adapter.sh
  - modelstore/lib/ollama_adapter.sh
  - example.bash_aliases
  - README.md
  - CHANGELOG.md
  - .gitignore
autonomous: false
requirements: [CLI-02, CLI-06, DOCS-01, DOCS-02, DOCS-03, DOCS-04]

must_haves:
  truths:
    - "All scripts in inference/, data/, eval/, containers/, setup/ subdirectories execute correctly (bash -n passes, lib.sh source path updated)"
    - "example.bash_aliases references new subdirectory paths and includes modelstore alias"
    - "README.md contains a Model Store section with subcommands table and NVIDIA Sync entry"
    - "CHANGELOG.md has a modelstore release entry"
    - ".gitignore excludes modelstore test artifacts"
    - "rsync --info=progress2 is used only when stdout is a TTY in adapter functions"
    - "Cron scripts in modelstore/cron/ work headless without TTY"
  artifacts:
    - path: "inference/"
      provides: "Inference launcher scripts (moved from root)"
    - path: "data/"
      provides: "Data engineering scripts (moved from root)"
    - path: "eval/"
      provides: "Evaluation scripts (moved from root)"
    - path: "containers/"
      provides: "Container launcher scripts (moved from root)"
    - path: "setup/"
      provides: "Setup scripts (moved from root)"
    - path: "example.bash_aliases"
      provides: "Updated aliases with new paths + modelstore alias"
    - path: "README.md"
      provides: "Updated documentation with Model Store section and corrected paths"
    - path: "CHANGELOG.md"
      provides: "Release entry for modelstore"
    - path: ".gitignore"
      provides: "Exclusions for modelstore runtime/test artifacts"
  key_links:
    - from: "example.bash_aliases"
      to: "inference/, data/, eval/, containers/"
      via: "alias path references"
      pattern: "~/dgx-toolbox/(inference|data|eval|containers)/"
    - from: "README.md"
      to: "modelstore.sh"
      via: "NVIDIA Sync custom app table"
      pattern: "modelstore.sh status"
    - from: "modelstore/lib/hf_adapter.sh"
      to: "rsync"
      via: "TTY-guarded progress flag"
      pattern: "info=progress2"
---

<objective>
Reorganize project root into category subdirectories, add progress bar TTY guards, and update all documentation.

Purpose: The root directory has 20+ scripts that should be organized into inference/, data/, eval/, containers/, setup/ subdirectories. All references (aliases, README, Sync commands) must be updated atomically. Progress bars need TTY guards for headless compatibility. Documentation must reflect the final state.

Output: Reorganized directory structure, updated aliases/README/CHANGELOG/.gitignore, TTY-guarded progress bars
</objective>

<execution_context>
@/home/robert_li/.claude/get-shit-done/workflows/execute-plan.md
@/home/robert_li/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-cli-status-revert-and-docs/04-CONTEXT.md
@.planning/phases/04-cli-status-revert-and-docs/04-RESEARCH.md
@.planning/phases/04-cli-status-revert-and-docs/04-01-SUMMARY.md

<interfaces>
<!-- Scripts that source lib.sh and need path update when moved to subdirs -->
<!-- These 5 scripts use: source "$(dirname "$0")/lib.sh" -->
<!-- After moving to subdirs, must change to: source "$(dirname "$0")/../lib.sh" -->

Scripts that source lib.sh (MUST update source path):
- start-open-webui.sh -> inference/start-open-webui.sh
- start-open-webui-sync.sh -> inference/start-open-webui-sync.sh
- start-n8n.sh -> containers/start-n8n.sh
- start-label-studio.sh -> data/start-label-studio.sh
- start-argilla.sh -> data/start-argilla.sh
- status.sh -> stays in root (no change needed)

Scripts that do NOT source lib.sh (just move, no source line edit):
- start-vllm.sh, start-vllm-sync.sh, start-litellm.sh, start-litellm-sync.sh,
  setup-litellm-config.sh, setup-ollama-remote.sh, triton-trtllm.sh,
  triton-trtllm-sync.sh, unsloth-studio.sh, unsloth-studio-sync.sh,
  data-toolbox.sh, data-toolbox-build.sh, data-toolbox-jupyter.sh,
  eval-toolbox.sh, eval-toolbox-build.sh, eval-toolbox-jupyter.sh,
  ngc-pytorch.sh, ngc-jupyter.sh, ngc-quickstart.sh, dgx-global-base-setup.sh

Docker compose files: do NOT reference any scripts by path (verified).
Keep in root: README.md, CHANGELOG.md, .gitignore, example.bash_aliases,
  example.vllm-model, lib.sh, modelstore.sh, status.sh, build-toolboxes.sh,
  docker-compose.*.yml

From modelstore/lib/hf_adapter.sh (progress bar location):
- rsync call in hf_migrate_model and hf_recall_model

From modelstore/lib/ollama_adapter.sh (progress bar location):
- rsync call in ollama_migrate_model and ollama_recall_model
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reorganize root into subdirectories and add progress bar TTY guards</name>
  <files>
    inference/start-open-webui.sh, inference/start-open-webui-sync.sh,
    inference/start-vllm.sh, inference/start-vllm-sync.sh,
    inference/start-litellm.sh, inference/start-litellm-sync.sh,
    inference/setup-litellm-config.sh, inference/setup-ollama-remote.sh,
    data/data-toolbox.sh, data/data-toolbox-build.sh, data/data-toolbox-jupyter.sh,
    data/start-label-studio.sh, data/start-argilla.sh,
    eval/eval-toolbox.sh, eval/eval-toolbox-build.sh, eval/eval-toolbox-jupyter.sh,
    eval/triton-trtllm.sh, eval/triton-trtllm-sync.sh,
    containers/ngc-pytorch.sh, containers/ngc-jupyter.sh, containers/ngc-quickstart.sh,
    containers/unsloth-studio.sh, containers/unsloth-studio-sync.sh,
    containers/start-n8n.sh,
    setup/dgx-global-base-setup.sh,
    modelstore/lib/hf_adapter.sh, modelstore/lib/ollama_adapter.sh
  </files>
  <read_first>
    example.bash_aliases
    lib.sh
    start-open-webui.sh
    start-open-webui-sync.sh
    start-n8n.sh
    start-label-studio.sh
    start-argilla.sh
    modelstore/lib/hf_adapter.sh
    modelstore/lib/ollama_adapter.sh
  </read_first>
  <action>
    PART A: Root reorganization (git mv for each script)

    Create subdirectories and move scripts using git mv:

    inference/:
      git mv start-open-webui.sh inference/
      git mv start-open-webui-sync.sh inference/
      git mv start-vllm.sh inference/
      git mv start-vllm-sync.sh inference/
      git mv start-litellm.sh inference/
      git mv start-litellm-sync.sh inference/
      git mv setup-litellm-config.sh inference/
      git mv setup-ollama-remote.sh inference/

    data/:
      git mv data-toolbox.sh data/
      git mv data-toolbox-build.sh data/
      git mv data-toolbox-jupyter.sh data/
      git mv start-label-studio.sh data/
      git mv start-argilla.sh data/

    eval/:
      git mv eval-toolbox.sh eval/
      git mv eval-toolbox-build.sh eval/
      git mv eval-toolbox-jupyter.sh eval/
      git mv triton-trtllm.sh eval/
      git mv triton-trtllm-sync.sh eval/

    containers/:
      git mv ngc-pytorch.sh containers/
      git mv ngc-jupyter.sh containers/
      git mv ngc-quickstart.sh containers/
      git mv unsloth-studio.sh containers/
      git mv unsloth-studio-sync.sh containers/
      git mv start-n8n.sh containers/

    setup/:
      git mv dgx-global-base-setup.sh setup/

    After moves, update lib.sh source path in the 5 scripts that source it.
    Change: source "$(dirname "$0")/lib.sh"
    To:     source "$(dirname "$0")/../lib.sh"
    In these files ONLY:
      inference/start-open-webui.sh
      inference/start-open-webui-sync.sh
      containers/start-n8n.sh
      data/start-label-studio.sh
      data/start-argilla.sh

    Verify each moved script with bash -n to catch source path errors:
      for f in inference/*.sh data/*.sh eval/*.sh containers/*.sh setup/*.sh; do bash -n "$f" || echo "FAIL: $f"; done

    PART B: Progress bar TTY guards (CLI-06)

    In modelstore/lib/hf_adapter.sh, find the rsync call(s) and wrap with TTY check:
    - If [[ -t 1 ]]; then use rsync -a --info=progress2; else use rsync -a (no progress)
    - Same pattern in modelstore/lib/ollama_adapter.sh

    In modelstore/lib/hf_adapter.sh, the hf_migrate_model function uses rsync. Add:
    ```
    local rsync_flags="-a"
    [[ -t 1 ]] && rsync_flags+=" --info=progress2"
    rsync $rsync_flags ...
    ```

    Same for hf_recall_model rsync call, ollama_migrate_model, and ollama_recall_model.

    Verify:
      grep -n "info=progress2" modelstore/lib/hf_adapter.sh modelstore/lib/ollama_adapter.sh
      # Should show TTY-guarded lines
  </action>
  <verify>
    <automated>for f in inference/*.sh data/*.sh eval/*.sh containers/*.sh setup/*.sh; do bash -n "$f" 2>&1 || echo "SYNTAX FAIL: $f"; done && grep -q 'info=progress2' modelstore/lib/hf_adapter.sh && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - test -d inference && test -d data && test -d eval && test -d containers && test -d setup
    - test -f inference/start-vllm.sh
    - test -f data/data-toolbox.sh
    - test -f eval/eval-toolbox.sh
    - test -f containers/ngc-pytorch.sh
    - test -f setup/dgx-global-base-setup.sh
    - grep -q '../lib.sh' inference/start-open-webui.sh
    - grep -q '../lib.sh' containers/start-n8n.sh
    - grep -q '../lib.sh' data/start-label-studio.sh
    - grep -q 'info=progress2' modelstore/lib/hf_adapter.sh
    - grep -q '\-t 1' modelstore/lib/hf_adapter.sh (TTY check)
    - test ! -f start-vllm.sh (no longer in root)
    - test ! -f start-open-webui.sh (no longer in root)
    - bash -n inference/start-open-webui.sh exits 0
    - bash -n containers/start-n8n.sh exits 0
  </acceptance_criteria>
  <done>
    All launcher scripts moved to category subdirectories. Scripts that source lib.sh have updated paths. All scripts pass bash -n syntax check. Adapter rsync calls use --info=progress2 only when stdout is a TTY.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update documentation (README, CHANGELOG, .gitignore, aliases)</name>
  <files>example.bash_aliases, README.md, CHANGELOG.md, .gitignore</files>
  <read_first>
    example.bash_aliases
    README.md
    CHANGELOG.md
    .gitignore
    modelstore.sh
  </read_first>
  <action>
    PART A: Update example.bash_aliases

    Update ALL alias paths from root to new subdirectory locations:
    - ~/dgx-toolbox/start-open-webui.sh -> ~/dgx-toolbox/inference/start-open-webui.sh
    - ~/dgx-toolbox/start-vllm.sh -> ~/dgx-toolbox/inference/start-vllm.sh
    - ~/dgx-toolbox/start-litellm.sh -> ~/dgx-toolbox/inference/start-litellm.sh
    - ~/dgx-toolbox/setup-litellm-config.sh -> ~/dgx-toolbox/inference/setup-litellm-config.sh
    - ~/dgx-toolbox/setup-ollama-remote.sh -> ~/dgx-toolbox/inference/setup-ollama-remote.sh
    - ~/dgx-toolbox/unsloth-studio.sh -> ~/dgx-toolbox/containers/unsloth-studio.sh
    - ~/dgx-toolbox/ngc-pytorch.sh -> ~/dgx-toolbox/containers/ngc-pytorch.sh
    - ~/dgx-toolbox/ngc-jupyter.sh -> ~/dgx-toolbox/containers/ngc-jupyter.sh
    - ~/dgx-toolbox/data-toolbox-build.sh -> ~/dgx-toolbox/data/data-toolbox-build.sh
    - ~/dgx-toolbox/data-toolbox.sh -> ~/dgx-toolbox/data/data-toolbox.sh
    - ~/dgx-toolbox/data-toolbox-jupyter.sh -> ~/dgx-toolbox/data/data-toolbox-jupyter.sh
    - ~/dgx-toolbox/start-label-studio.sh -> ~/dgx-toolbox/data/start-label-studio.sh
    - ~/dgx-toolbox/start-argilla.sh -> ~/dgx-toolbox/data/start-argilla.sh
    - ~/dgx-toolbox/eval-toolbox-build.sh -> ~/dgx-toolbox/eval/eval-toolbox-build.sh
    - ~/dgx-toolbox/eval-toolbox.sh -> ~/dgx-toolbox/eval/eval-toolbox.sh
    - ~/dgx-toolbox/eval-toolbox-jupyter.sh -> ~/dgx-toolbox/eval/eval-toolbox-jupyter.sh
    - ~/dgx-toolbox/triton-trtllm.sh -> ~/dgx-toolbox/eval/triton-trtllm.sh
    - ~/dgx-toolbox/start-n8n.sh -> ~/dgx-toolbox/containers/start-n8n.sh

    Add modelstore alias at end of file (before Utilities section or as new section):
    ```
    # --- Model Store ---
    alias modelstore='~/dgx-toolbox/modelstore.sh'               # Tiered model storage manager
    ```

    PART B: Update README.md

    1. Add "Model Store" section after existing tool sections. Include:
       - Brief description (2-3 sentences about tiered storage)
       - Subcommands table:
         | Command | Description |
         |---------|-------------|
         | modelstore init | Interactive setup wizard |
         | modelstore status | Show models by tier with sizes |
         | modelstore migrate | Move stale models hot->cold |
         | modelstore migrate --dry-run | Preview what would migrate |
         | modelstore recall | Move model cold->hot |
         | modelstore revert | Move all models back to hot |
         | modelstore revert --force | Revert without confirmation |
       - Quick start: run modelstore init, check modelstore status, migration runs on cron

    2. Update NVIDIA Sync custom app table in README:
       - Add entry for modelstore status: Command = "bash ~/dgx-toolbox/modelstore.sh status", Port = "--" (none), Auto-open = No
       - Update all existing Sync command paths from root to subdirectory paths (e.g., start-vllm-sync.sh -> inference/start-vllm-sync.sh)

    3. Update all script path references throughout README to use new subdirectory paths

    PART C: Update CHANGELOG.md

    Add new top-level entry at the beginning:

    ```
    ## 2026-03-22 -- Model Store

    ### Added

    - **modelstore.sh** -- Tiered model storage CLI (init, status, migrate, recall, revert)
    - **modelstore/cmd/status.sh** -- Dashboard showing all models by tier with sizes, last-used timestamps, drive totals, watcher/cron status
    - **modelstore/cmd/revert.sh** -- Interrupt-safe full revert with preview, --force flag, cleanup of cron/watcher/cold dirs
    - **modelstore/cmd/migrate.sh** -- Automated hot-to-cold migration with dry-run, stale detection, flock concurrency guard
    - **modelstore/cmd/recall.sh** -- Cold-to-hot recall with usage timestamp reset, auto-trigger from watcher
    - **modelstore/cmd/init.sh** -- Interactive setup wizard with filesystem validation, model scan, cron install
    - Tiered storage automation via cron (migrate stale models, disk space alerts)
    - Usage tracking via docker events + inotifywait watcher daemon
    - HuggingFace and Ollama storage adapters with safety guards

    ### Changed

    - Reorganized project root into subdirectories: inference/, data/, eval/, containers/, setup/
    - Updated example.bash_aliases with new script paths and modelstore alias
    ```

    PART D: Update .gitignore

    Add modelstore test artifact exclusions:
    ```
    # Modelstore test artifacts
    modelstore/test/tmp*
    modelstore/test/fixtures/tmp*
    ```

    PART E: Do NOT modify ~/.bash_aliases (it's a user file outside the repo). The example.bash_aliases serves as the template. Add a comment at the top of example.bash_aliases reminding the user to re-copy:
    ```
    # Copy to ~/.bash_aliases:  cp ~/dgx-toolbox/example.bash_aliases ~/.bash_aliases && source ~/.bash_aliases
    ```
    (This line already exists -- just ensure it remains.)
  </action>
  <verify>
    <automated>grep -q 'modelstore' example.bash_aliases && grep -q 'Model Store' README.md && grep -q 'modelstore' CHANGELOG.md && grep -q 'modelstore/test' .gitignore && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - grep -q 'inference/start-vllm.sh' example.bash_aliases
    - grep -q 'inference/start-open-webui.sh' example.bash_aliases
    - grep -q "alias modelstore=" example.bash_aliases
    - grep -q 'Model Store' README.md
    - grep -q 'modelstore init' README.md
    - grep -q 'modelstore status' README.md
    - grep -q 'modelstore revert' README.md
    - grep -q 'modelstore.sh status' README.md (NVIDIA Sync entry)
    - grep -q 'Model Store' CHANGELOG.md
    - grep -q 'Reorganized project root' CHANGELOG.md
    - grep -q 'modelstore/test/tmp' .gitignore
    - No old root paths remain in example.bash_aliases: ! grep -q "~/dgx-toolbox/start-vllm.sh" example.bash_aliases
  </acceptance_criteria>
  <done>
    All documentation updated: example.bash_aliases has new paths + modelstore alias, README has Model Store section + updated Sync table + corrected paths, CHANGELOG has release entry, .gitignore excludes test artifacts.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify reorganization and documentation</name>
  <files>README.md, example.bash_aliases, CHANGELOG.md, .gitignore</files>
  <action>
    Human verification checkpoint. All automation is complete in Tasks 1 and 2.
    Verify: directory reorganization is correct, documentation is accurate, all paths updated.
  </action>
  <verify>
    Human checks:
    1. ls inference/ data/ eval/ containers/ setup/ -- scripts in correct subdirectories
    2. bash -n inference/start-open-webui.sh -- lib.sh source path works
    3. README.md Model Store section exists with subcommands table and Sync entry
    4. example.bash_aliases paths point to subdirectories, modelstore alias exists
    5. bash modelstore/test/run-all.sh -- no regressions
  </verify>
  <done>User has approved the reorganization and documentation updates.</done>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
# Verify reorganization
for dir in inference data eval containers setup; do test -d "$dir" && echo "$dir OK"; done

# Verify no scripts left in root that should have been moved
test ! -f start-vllm.sh && test ! -f start-open-webui.sh && test ! -f ngc-pytorch.sh && echo "Root clean"

# Verify documentation
grep -q 'Model Store' README.md && grep -q 'modelstore' example.bash_aliases && echo "Docs OK"

# Full test suite
bash modelstore/test/run-all.sh
</verification>

<success_criteria>
- All launcher scripts organized into inference/, data/, eval/, containers/, setup/
- Scripts that source lib.sh have updated paths (../lib.sh) and pass bash -n
- example.bash_aliases has all paths updated + modelstore alias
- README has Model Store section with subcommands, Sync entry, corrected paths
- CHANGELOG has modelstore release entry
- .gitignore excludes modelstore test artifacts
- rsync uses --info=progress2 only when stdout is TTY
- Full test suite passes
</success_criteria>

<output>
After completion, create `.planning/phases/04-cli-status-revert-and-docs/04-02-SUMMARY.md`
</output>
