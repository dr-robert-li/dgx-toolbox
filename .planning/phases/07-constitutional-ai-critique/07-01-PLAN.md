---
phase: 07-constitutional-ai-critique
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - harness/critique/__init__.py
  - harness/critique/constitution.py
  - harness/config/rail_loader.py
  - harness/config/rails/rails.yaml
  - harness/config/constitution.yaml
  - harness/tests/test_constitution.py
  - harness/tests/test_rail_config.py
autonomous: true
requirements: [CSTL-02, CSTL-04]

must_haves:
  truths:
    - "Valid constitution.yaml loads at startup and produces a ConstitutionConfig with typed Principle objects"
    - "Malformed constitution.yaml raises ValueError at startup before the service accepts traffic"
    - "Disabled principles (enabled: false) are filterable from the principle list"
    - "critique_threshold on an output rail defines the borderline risk band between critique_threshold and threshold"
    - "critique_threshold >= threshold is rejected at startup with a clear error"
    - "Input rails have no critique_threshold (output-only feature)"
  artifacts:
    - path: "harness/critique/constitution.py"
      provides: "ConstitutionConfig, Principle, ConstitutionFile Pydantic models; load_constitution()"
      exports: ["ConstitutionConfig", "Principle", "ConstitutionFile", "load_constitution"]
    - path: "harness/critique/__init__.py"
      provides: "Package init exporting public API"
    - path: "harness/config/constitution.yaml"
      provides: "Default 10-15 principles covering safety, fairness, accuracy, helpfulness"
      contains: "constitution:"
    - path: "harness/config/rail_loader.py"
      provides: "RailConfig with critique_threshold field and cross-field validator"
      contains: "critique_threshold"
    - path: "harness/config/rails/rails.yaml"
      provides: "critique_threshold on output rails"
      contains: "critique_threshold"
    - path: "harness/tests/test_constitution.py"
      provides: "Tests for constitution loading, validation, disabled principle filtering"
    - path: "harness/tests/test_rail_config.py"
      provides: "Test for critique_threshold >= threshold rejection"
  key_links:
    - from: "harness/critique/constitution.py"
      to: "harness/config/constitution.yaml"
      via: "load_constitution reads YAML and validates via Pydantic"
      pattern: "yaml\\.safe_load.*ConstitutionFile\\.model_validate"
    - from: "harness/config/rail_loader.py"
      to: "harness/config/rails/rails.yaml"
      via: "load_rails_config parses critique_threshold"
      pattern: "critique_threshold.*Optional\\[float\\]"
---

<objective>
Create the configuration foundation for Constitutional AI: Pydantic-validated constitution.yaml with categorized principles, and critique_threshold extension on RailConfig for risk gating.

Purpose: All downstream plans (CritiqueEngine, Analyzer) depend on these types and config files. Constitution loading and validation at startup prevents misconfiguration from reaching production.

Output: harness/critique/ package with constitution.py, default constitution.yaml, extended RailConfig, and tests.
</objective>

<execution_context>
@/home/robert_li/.claude/get-shit-done/workflows/execute-plan.md
@/home/robert_li/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-constitutional-ai-critique/07-CONTEXT.md
@.planning/phases/07-constitutional-ai-critique/07-RESEARCH.md

<interfaces>
<!-- Existing types and contracts the executor needs -->

From harness/config/rail_loader.py:
```python
class RailConfig(BaseModel):
    name: str
    enabled: bool = True
    threshold: float = 0.7
    refusal_mode: Literal["hard_block", "soft_steer", "informative"] = "hard_block"

class RailsFile(BaseModel):
    rails: List[RailConfig]

def load_rails_config(config_path: str) -> List[RailConfig]:
```

From harness/guards/types.py:
```python
@dataclass
class RailResult:
    rail: str
    result: str       # "pass" | "block"
    score: float
    threshold: float

@dataclass
class GuardrailDecision:
    blocked: bool
    refusal_mode: Optional[str]
    triggering_rail: Optional[str]
    all_results: list[RailResult]
    replacement_response: Optional[dict] = None
    evasion_flags: list[str] = field(default_factory=list)
```

From harness/config/loader.py:
```python
class TenantConfig(BaseModel):
    tenant_id: str
    api_key_hash: str
    rpm_limit: int = 60
    tpm_limit: int = 100_000
    allowed_models: List[str] = ["*"]
    bypass: bool = False
    pii_strictness: str = "balanced"
    rail_overrides: Dict[str, Dict[str, object]] = {}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Constitution config types, loader, and default YAML</name>
  <files>harness/critique/__init__.py, harness/critique/constitution.py, harness/config/constitution.yaml, harness/tests/test_constitution.py</files>
  <read_first>
    - harness/config/rail_loader.py (pattern to mirror for Pydantic YAML validation)
    - harness/config/loader.py (TenantConfig pattern for reference)
    - harness/config/rails/rails.yaml (existing config format reference)
    - harness/tests/test_rail_config.py (test pattern to mirror)
  </read_first>
  <behavior>
    - test_load_valid_constitution: load_constitution("path/to/valid.yaml") returns ConstitutionConfig with judge_model="default" and a list of Principle objects
    - test_malformed_constitution_raises: load_constitution on YAML missing required "principles" key raises ValueError
    - test_empty_constitution_raises: load_constitution on empty YAML file raises ValueError
    - test_invalid_principle_raises: Principle with missing "text" field raises ValueError during load
    - test_disabled_principle_excluded: ConstitutionConfig with 3 principles (2 enabled, 1 disabled); filtering by enabled=True returns 2
    - test_principle_priority_ordering: Principles can be sorted by priority descending (highest first)
    - test_judge_model_configurable: constitution.yaml with judge_model="llama3.1-70b" loads correctly
    - test_default_constitution_loads: The shipped harness/config/constitution.yaml loads without error and has >= 8 principles
  </behavior>
  <action>
    1. Create harness/critique/__init__.py with exports: ConstitutionConfig, Principle, load_constitution.

    2. Create harness/critique/constitution.py with:
       - class Principle(BaseModel): id: str, text: str, category: str, priority: float, enabled: bool = True
       - class ConstitutionConfig(BaseModel): judge_model: str = "default", principles: List[Principle]
       - class ConstitutionFile(BaseModel): constitution: ConstitutionConfig
       - def load_constitution(config_path: str) -> ConstitutionConfig:
         Opens YAML, validates via ConstitutionFile.model_validate(raw), returns .constitution.
         Raises ValueError on empty, malformed, or schema-invalid YAML.
         Mirrors harness/config/rail_loader.py:load_rails_config() pattern exactly.

    3. Create harness/config/constitution.yaml with 12 default principles:
       - Safety (4): P-SAFETY-01 (no harm instructions, priority 1.0), P-SAFETY-02 (no minor exploitation, 0.95), P-SAFETY-03 (no dangerous substance synthesis, 0.90), P-SAFETY-04 (no weapon creation assistance, 0.90)
       - Fairness (3): P-FAIRNESS-01 (equitable treatment, 0.85), P-FAIRNESS-02 (no dehumanization, 0.80), P-FAIRNESS-03 (avoid cultural bias, 0.75)
       - Accuracy (3): P-ACCURACY-01 (no speculation as fact, 0.75), P-ACCURACY-02 (acknowledge uncertainty, 0.70), P-ACCURACY-03 (no fabricated citations, 0.70)
       - Helpfulness (2): P-HELPFULNESS-01 (offer safe alternatives, 0.60), P-HELPFULNESS-02 (no excessive caution, 0.55)
       All enabled: true. judge_model: default.

    4. Create harness/tests/test_constitution.py with the behavior tests above. Use tmp_path for writing test YAML files. The test_default_constitution_loads test uses the real harness/config/constitution.yaml path via Path(__file__).parent.parent / "config" / "constitution.yaml".
  </action>
  <verify>
    <automated>cd /home/robert_li/dgx-toolbox/harness && python -m pytest tests/test_constitution.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - harness/critique/__init__.py contains "from harness.critique.constitution import"
    - harness/critique/constitution.py contains "class Principle(BaseModel):"
    - harness/critique/constitution.py contains "class ConstitutionConfig(BaseModel):"
    - harness/critique/constitution.py contains "class ConstitutionFile(BaseModel):"
    - harness/critique/constitution.py contains "def load_constitution("
    - harness/critique/constitution.py contains 'raise ValueError(' (at least 2 occurrences: empty YAML, schema validation)
    - harness/config/constitution.yaml contains "constitution:"
    - harness/config/constitution.yaml contains "P-SAFETY-01"
    - harness/config/constitution.yaml contains "P-FAIRNESS-01"
    - harness/config/constitution.yaml contains "P-ACCURACY-01"
    - harness/config/constitution.yaml contains "P-HELPFULNESS-01"
    - harness/config/constitution.yaml contains "judge_model: default"
    - harness/tests/test_constitution.py contains "test_load_valid_constitution"
    - harness/tests/test_constitution.py contains "test_malformed_constitution_raises"
    - harness/tests/test_constitution.py contains "test_disabled_principle_excluded"
    - harness/tests/test_constitution.py contains "test_default_constitution_loads"
    - python -m pytest tests/test_constitution.py -x -q exits 0
  </acceptance_criteria>
  <done>ConstitutionConfig and Principle Pydantic models exist with load_constitution(). Default constitution.yaml has 12 principles in 4 categories. All 8 tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend RailConfig with critique_threshold and cross-field validation</name>
  <files>harness/config/rail_loader.py, harness/config/rails/rails.yaml, harness/tests/test_rail_config.py</files>
  <read_first>
    - harness/config/rail_loader.py (current state — will be modified)
    - harness/config/rails/rails.yaml (current state — will be modified)
    - harness/tests/test_rail_config.py (current state — will be extended)
  </read_first>
  <behavior>
    - test_critique_threshold_valid: RailConfig(name="x", threshold=0.7, critique_threshold=0.5) validates successfully
    - test_critique_threshold_none_valid: RailConfig(name="x", threshold=0.7, critique_threshold=None) validates successfully (no critique for this rail)
    - test_critique_threshold_invalid: RailConfig(name="x", threshold=0.7, critique_threshold=0.9) raises ValidationError (critique_threshold >= threshold)
    - test_critique_threshold_equal_invalid: RailConfig(name="x", threshold=0.7, critique_threshold=0.7) raises ValidationError (must be strictly less)
    - test_rails_yaml_loads_with_critique_threshold: load_rails_config on updated rails.yaml succeeds and output rails have critique_threshold set
    - test_input_rails_no_critique_threshold: Input rails in loaded config have critique_threshold=None
  </behavior>
  <action>
    1. Modify harness/config/rail_loader.py:
       - Add import: from pydantic import BaseModel, ValidationError, model_validator
       - Add import: from typing import Optional (already has List, Literal)
       - Add field to RailConfig: critique_threshold: Optional[float] = None
       - Add @model_validator(mode='after') to RailConfig:
         ```python
         @model_validator(mode='after')
         def validate_critique_threshold(self) -> 'RailConfig':
             if self.critique_threshold is not None:
                 if self.critique_threshold >= self.threshold:
                     raise ValueError(
                         f"Rail '{self.name}': critique_threshold ({self.critique_threshold}) "
                         f"must be less than threshold ({self.threshold})"
                     )
             return self
         ```

    2. Modify harness/config/rails/rails.yaml: Add critique_threshold to output rails ONLY:
       - self_check_output: critique_threshold: 0.5 (existing threshold: 0.7)
       - jailbreak_output: critique_threshold: 0.5 (existing threshold: 0.7)
       - sensitive_data_output: critique_threshold: 0.15 (existing threshold: 0.2)
       Input rails (self_check_input, jailbreak_detection, sensitive_data_input, injection_heuristic) remain unchanged — no critique_threshold.

    3. Add tests to harness/tests/test_rail_config.py (append to existing file):
       - test_critique_threshold_valid, test_critique_threshold_none_valid
       - test_critique_threshold_invalid, test_critique_threshold_equal_invalid
       - test_rails_yaml_loads_with_critique_threshold (uses real rails.yaml)
       - test_input_rails_no_critique_threshold (uses real rails.yaml)
  </action>
  <verify>
    <automated>cd /home/robert_li/dgx-toolbox/harness && python -m pytest tests/test_rail_config.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - harness/config/rail_loader.py contains "critique_threshold: Optional[float] = None"
    - harness/config/rail_loader.py contains "@model_validator(mode='after')"
    - harness/config/rail_loader.py contains "critique_threshold ({self.critique_threshold}) must be less than threshold"
    - harness/config/rails/rails.yaml contains "critique_threshold: 0.5" (for self_check_output)
    - harness/config/rails/rails.yaml contains "critique_threshold: 0.15" (for sensitive_data_output)
    - harness/tests/test_rail_config.py contains "test_critique_threshold_invalid"
    - harness/tests/test_rail_config.py contains "test_critique_threshold_equal_invalid"
    - harness/tests/test_rail_config.py contains "test_rails_yaml_loads_with_critique_threshold"
    - python -m pytest tests/test_rail_config.py -x -q exits 0
    - python -m pytest tests/ -x -q exits 0 (full suite still passes)
  </acceptance_criteria>
  <done>RailConfig has Optional critique_threshold with cross-field validator. Output rails in rails.yaml have critique_threshold values. Input rails have None. All existing + 6 new tests pass.</done>
</task>

</tasks>

<verification>
cd /home/robert_li/dgx-toolbox/harness && python -m pytest tests/test_constitution.py tests/test_rail_config.py -x -q
cd /home/robert_li/dgx-toolbox/harness && python -m pytest tests/ -x -q
</verification>

<success_criteria>
- ConstitutionConfig, Principle, load_constitution() exist in harness/critique/constitution.py
- Default constitution.yaml has 12 principles in 4 categories (safety, fairness, accuracy, helpfulness)
- Malformed constitution.yaml raises ValueError at load time
- RailConfig has critique_threshold: Optional[float] with cross-field validation
- Output rails in rails.yaml have critique_threshold; input rails do not
- All tests pass (new + existing)
</success_criteria>

<output>
After completion, create `.planning/phases/07-constitutional-ai-critique/07-01-SUMMARY.md`
</output>
